These are some example scripts on how to batch generate, build and test the big int routines.
First, build the genbigint.exe (Release version) using the visual studio project.
Then, run these batch files from a x64 native tools command prompt (cd first to the scripts dir).

